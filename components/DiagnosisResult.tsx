import React from 'react';

// This component was part of the original plant agent. The FORMA Coach flow uses
// `CoachingResult` instead. Keep a harmless placeholder here to avoid import errors.
export const DiagnosisResult: React.FC = () => null;
